{
  "name": "cohort15-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cohort15 MVP web app foundation.",
  "scripts": {
    "dev": "node src/server/app.mjs",
    "start": "node src/server/app.mjs",
    "lint": "node scripts/lint.mjs",
    "check:workflow": "node scripts/check-agent-workflow.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run lint && npm run check:workflow && npm test"
  },
  "engines": {
    "node": ">=24"
  }
}
